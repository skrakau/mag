# You can use this file to create a conda environment for this pipeline:
#   conda env create -f environment.yml
name: nf-core-mag-centrifuge-1.1.0dev
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - conda-forge::python=2.7.15
  - bioconda::centrifuge=1.0.4_beta # no conda build for python 3.7.3 (only for 3.6), python2 needed for auxiliary scripts

